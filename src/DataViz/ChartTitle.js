import React from 'react'

export const ChartTitle = ({children}) => {
  return (
    <h3>{children}</h3>
  )
}

export const ChartSubTitle = ({children}) => {
  return (
    <h4>{children}</h4>
  )
}
